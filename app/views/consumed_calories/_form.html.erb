<%= form_for(@calorie) do |f| %>
  <% if @calorie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@calorie.errors.count, "error") %> prohibited this calorie journal entry from being saved:</h2>

      <ul>
      <% @calorie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :total %><br>
    <%= f.text_field :total %>
  </div>
  <div class="field">
    <%= f.label :consumed_on %><br>
    <%= f.date_select :consumed_on %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
